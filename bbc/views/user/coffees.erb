<!DOCTYPE html>

 <% if p.Bean == false %>
  <div class="product-box <%= 'out-of-stock' if p.StockQuantity.to_i <= 0 %>">  

    <% if p.StockQuantity.to_i <= 0 %>
      <div class="sold-out-banner">OUT OF STOCK</div>
    <% end %>
    <img src="/imgs/<%= p.ProductImage %>" alt="Coffee" class="product-image">
    <h3><%= p.ProductName %></h3>
    <p><%= p.ProductDescription %></p>
    <p>Roast: <%= p.Roast %></p>
    <p>Origin: <%= p.Origin %></p>
    <b>£<%= p.Price %></b>
    <form action="/user/shop" method="post">
      <input type="hidden" name="productId" value="<%= p.ProductId %>">
      <input type="hidden" name="userId" value="<%= session[:userId]%>">
      
      <label for="quantity">Select a quantity</label>
        <select name="quantity">
          <%max_available = [p.StockQuantity.to_i, 10].min %>
          <% (1..max_available).each do |q| %>
            <option value="<%= q %>"><%= q %></option>
          <% end %>
        </select>
       <label for="Size">Size</label>
          <p></p>
            <select name="size">
              <option value="Small">Small</option>
               <option value="Medium">Medium (+£1)</option>
              <option value="Large">Large (+£2)</option>
             </select>
          <p></p>
          <label for="milk">Milk Type</label>
          <p></p>
            <select name="milk">
              <option value="Whole Milk">Whole Milk</option>
              <option value="Soy Milk">Soy Milk (+£1)</option>
              <option value="Oat Milk">Oat Milk (+£2)</option>
            </select>
      <button type="submit" style="cursor: pointer;">Add to basket</button>
    </form>
  </div>  
<% end %>